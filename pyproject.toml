[tool.ruff]
target-version = "py311"
line-length = 100
select = ["E", "F", "I", "UP", "B"]
ignore = ["E501", "B008"]
exclude = ["venv", ".venv", "tests"]

[tool.ruff.isort]
known-first-party = ["app"]
